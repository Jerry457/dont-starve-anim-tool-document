import type { SidebarConfig } from '@vuepress/theme-default'

export const sidebarEn: SidebarConfig = {
    "/": [
        {
            text: "Spine项目格式规范",
            link: "/zh/index.md",
            collapsible: true,
        },
    ],
}
